language: python
dist: xenial
python:
  - "3.7"

git:
  depth: false

jobs:
  include:
    - stage: test
      addons:
        sonarcloud:
          organization: "project-alice-assistant"
          token:
            secure: $SONAR_TOKEN
      script: sonar-scanner
